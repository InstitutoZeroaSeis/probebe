db:
  environment:
    MYSQL_ROOT_PASS: root
  hostname: probebe-mysql
  image: mysql
  ports:
    - 3306:3306
  volumes:
    - .data:/var/lib/mysql

web:
  command: bundle exec rails s
  image: rails
  links:
    - db
  ports:
    - 3000:3000
  volumes:
    - .:/app
  working_dir: /app
