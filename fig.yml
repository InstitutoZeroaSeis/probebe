db:
  environment:
    MYSQL_PASS: root
  image: tutum/mysql
  ports:
    - 3306:3306
  volumes:
    - .data:/var/lib/mysql

web:
  command: bundle exec rails s
  image: rails
  links:
    - db
  ports:
    - 3000:3000
  volumes:
    - .:/app
  working_dir: /app
