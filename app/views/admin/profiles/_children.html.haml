.row.children
  - if record.children.size > 0
    - record.children.each do |child|
      .child
        .row
          .col-md-12
            %label="#{t('activerecord.attributes.child.name')}: "
            .value="#{child.name}"
        .row
          .col-md-12
            %label="#{t('activerecord.attributes.child.birth_date')}: "
            .value="#{child.birth_date}"


