!!!5
%html
  %head
    %title ProBebê

    = favicon_link_tag
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'

    = csrf_meta_tags
    = render partial: 'flash_messages'

    = yield :head
  %body{class: "#{params[:controller]}"}
    .timeline-modal-bg
    .timeline-modal-close
    .timeline-modal
      oi
    = render 'sign_in'
    = render 'site_menu'
    %section.main.container-fuild
      = yield
    %footer.footer
      = render 'sign_up_footer', source: '' unless current_user
      %section.footer-copyright
        .container
          = render 'site_footer'

